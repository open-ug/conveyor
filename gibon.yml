# Commands used by the gibon autonomous coding agent to validate work during coding sessions
dependencies: go mod download # Download Go module dependencies
generate: make swagger-init # Generate Swagger API documentation from code comments  
lint: go vet ./... # Run Go static analysis and lint checks
unit: make test # Run unit tests with verbose output in test environment
build: go build -o conveyor . # Build the Conveyor binary executable