The Conveyor CI API Server can be accessed via port {{ .Values.service.ports.http }} on the following DNS name from within your cluster:
{{ include "conveyor.fullname" . }}.{{ include "conveyor.namespace" . }}.svc.cluster.local
